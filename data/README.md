[root](../README.md) / data

# Data

This directory holds non-code gameplay configuration.

## Structure

- `tuning/`: movement, vehicle, wanted, and mission tuning resources
- `missions/`: mission data resources
- `districts/`: district-specific data resources

Push frequently tuned values here instead of hardcoding them into gameplay scripts.
